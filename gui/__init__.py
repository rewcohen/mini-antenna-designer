"""Wizard-driven CAD-style GUI package for the Mini Antenna Designer.

New front end (step rail + live SVG canvas + properties panel) that reuses the
existing backend (core/design/design_generator/export/presets/storage). The
legacy single-file UI remains in ``ui.py`` until this package reaches parity.
"""
