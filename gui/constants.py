"""Shared layout constants for the wizard GUI.

Spacing scale (px) used for widget padding throughout ``gui/`` and ``app.py``.
Previously redefined in nearly every module; centralized here so the rhythm is
tuned in one place.
"""

PAD_S, PAD_M, PAD_L = 4, 8, 12
